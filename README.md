# cancion-diaria-telegram-bot

un bot de telegram que te manda un mensaje con la canción del día y también te arma un mix con las canciones de la última semana.

Para instalar todas las dependencias, correr en la terminal:

```bash
$ pip install -r requirements.txt
```